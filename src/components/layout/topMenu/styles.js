import { COLORS } from "../../../customStyles";

export default (theme) => ({
    appBar: {
        backgroundColor: COLORS.FDI_YELLOW,
    },
    title: {
        flexGrow: 1,
        color: "black",
    },
});
